node_modules/
dist/
*.local
.DS_Store

# TypeScript incremental build cache
*.tsbuildinfo

# Env / secrets (project uses none today — defense in depth)
.env
.env.*

# Ad-hoc local verification scripts/screenshots (never meant to be committed)
tmp-*

# Raw VDOT statewide exports worked with locally — never committed
*.xlsx

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
